# Optional on-device models

The deployable app works without bundled model binaries. Add licensed `.tflite` or `.onnx` models here and connect them through `AiService` for production classification and embeddings.
